# Journal: 2026-08-23

## Field Ownership

- User-only: `Today's Intentions`, `Notes / Reflections`
- Agent-managed: `Kickoff Context`, `TODO State Summary`, `Plan Checkpoint Linkage`, `Repo Work Log`
- Agents copy user-only text verbatim; without user text, leave `-`.

## Today's Intentions

-

## Kickoff Context

- Date: 2026-08-23
- Journal file status: created
- Root `TODO.md` status: found
- Kickoff goal: Establish, with code-level evidence, the true execution model of the Agentic Pipelines framework — specifically, what `pipeline.py run` actually does versus what `scripts/build.py` does, and whether the two are coupled.

## TODO State Summary

- Relevant root checklist items: build and governance verification
- Checklist edits this checkpoint: build verification completed; governance reviewer validation remains pending provider-cohort retry

## Plan Checkpoint Linkage

- Active plan: `plans/current/2026-08-19-10-55-32_build-pipeline-implementation.md`
- Checklist deltas: corrected execution-model contract and build-driver defects; preflight and local artifact verification completed
- Approved decisions: preserve the non-coupled build-executor and governance-harness design; do not reset quarantined governance entities automatically
- Open decisions: approve a failure-cohort retry after the local inference provider is available
- Evidence/report links:
  - `agentic-pipelines/pipeline_runtime/runner.py` — `_invoke()` is the single LLM bridge; `run()` is a pure orchestration loop; no subprocess/Popen/shell-out in the runtime
  - `agentic-pipelines/pipeline_runtime/packets.py` — `InferenceClient` issues HTTP calls with `InferenceRequest`; JSON-schema-constrained responses
  - `agentic-pipelines/scripts/pipeline.py` — CLI entrypoint; `_runner()` constructs `PipelineRunner(definition, InferenceClient(...))`
  - `scripts/build.py` — the actual build executor; `subprocess.run([go, "build", ...])` against a static 8-target (goos, goarch) matrix
  - `scripts/pipeline-bootstrap.ps1` — runs `pipeline.py` only; does NOT invoke `build.py`
  - `prompts/worker.md`, `prompts/self_review.md`, `prompts/reviewer.md` — worker asks the LLM to "execute the no-flag build" and return strict `worker_result`; reviewer: "Deterministic failures cannot be overridden"
  - `agentic-pipelines/schemas/prompt_outputs.schema.json` — `worker_result` / `reviewer_verdict` / `repair_result` / `adjudication_result` / `finding`
- Material risks: none identified; the framework is deterministic-plus-LLM-reasoning and does not itself perform host builds
- Exact next approved action: persist this journal entry and draft the corrected two-subsystem framing note for README/AGENTS.md

## Repo Work Log (Required)

- [2026-08-23 local] Confirmed, by reading `pipeline_runtime/runner.py`, `packets.py`, and `pipeline.py`, that `pipeline.py run` is a pure LLM-reasoning-plus-deterministic-validation engine. The sole bridge to an external model is `_invoke()` via `InferenceClient` (HTTP). No subprocess, Popen, or shell-out exists anywhere in the runtime.
- [2026-08-23 local] Confirmed that `scripts/build.py` is the standalone build executor (Go 1.26.4, Ebitengine v2.9.9) using `subprocess.run()` over a static target matrix, and that `scripts/pipeline-bootstrap.ps1` runs `pipeline.py` only and never calls `build.py`. The two subsystems are complementary but NOT coupled in the bootstrap flow.
- [2026-08-23 local] Reconciled the apparent contradiction in the worker prompt: it asks the LLM to "execute the build", but the framework cannot execute — the LLM emits a textual candidate constrained to `worker_result`, and deterministic validation gates the claim. Documented the confirmed two-subsystem model and recorded it for the corrected README/AGENTS.md framing.

- [2026-08-24 local] Corrected the build driver to invoke an actual Go executable, use host-local ignored Go caches, disable VCS stamping for this mixed-ownership checkout, serialize report paths safely, and return verification failure when a feasible target has no artifact. Corrected the artifact runner to resolve the declared target-directory layout and made both scripts UTF-8-safe on Windows consoles.
- [2026-08-24 local] Bootstrap preflight passed. `scripts/build.py --verify` built and verified `windows/amd64/windows-amd64/latest.exe` (9,687,040 bytes) and `js/wasm/js-wasm/latest.wasm` (10,251,712 bytes); six non-host CGO desktop targets were reported infeasible with reasons. Evidence: `reports/build/build-report-20260824T062303Z.json`.
- [2026-08-24 local] Governance discovery found the three Python build scripts and the run made no build subprocess calls. The configured local inference endpoint was blocked in the sandbox (`socket_permission_denied`), opening the run-local provider circuit and quarantining the entities. The elevated rerun correctly preserved that quarantine; no automatic retry or state reset was performed. Evidence: `reports/run-fc6a307521544317.json`.

- [2026-08-24 local] Replaced the placeholder app with a self-contained `cmd/app/main.go` demo: a draggable "Hello, world." label that flicks, bounces off all edges, and slows through friction. The source file has no supporting app files and is explicitly safe for a forker to replace or delete.
- [2026-08-24 local] Simplified generated artifact paths by separating the matrix name from the application target. Verified output now lands at `releases/windows/amd64/app/latest.exe` and `releases/js/wasm/app/latest.wasm`; removed the two obsolete generated `windows-amd64/` and `js-wasm/` directories.

- [2026-08-24 local] Expanded `scripts/build.py` preflight output to show the resolved Go executable and version, configured CGO compiler status, policy, artifact path, feasibility state, and concrete reason for all eight targets before any build begins. Verified the report and successful Windows AMD64 and JS/WASM artifact builds with `python scripts/build.py --verify`.

- [2026-08-24 local] With explicit approval, retried exactly the two saved provider-failure cohorts (three entities) after Xavier returned HTTP 200. The retry reached the worker model and preserved the no-build-execution boundary, but it correctly returned `unable`: each source entity supplies only one script, while a bounded target-matrix review needs the canonical `build.py` target definition and build-run evidence. The run quarantined the entities with semantic evidence at `reports/run-158da7c17c1443fc.json`; no automatic retry or promotion occurred. A revised aggregate-evidence pipeline contract requires separate approval.

- [2026-08-24 local] Reframed the boilerplate documentation: the no-flag local build is primary, deploy is a user-configured secondary pipeline, and `agentic-pipelines/` plus Ollama configuration are optional scaffolding. Agents must use local deterministic checks and this session's review by default, and must not contact an inference endpoint unless explicitly directed.

## Notes / Reflections

-
