# TODO

- [ ] Set up the secondary deploy pipeline: choose release destinations, packaging, signing, credentials, and CI before enabling publishing.
- [ ] Add project-specific deterministic build and cross-platform release validators with evaluation fixtures.
- [ ] Optional: configure `api.yaml` from `api.sample.yaml` only if you want to use the separate Ollama governance scaffold.
- [ ] Optional: add an aggregate-evidence governance contract if you want Ollama to review build reports rather than individual source files.
- [ ] Add GitHub Actions workflows only after the deploy pipeline's target platforms, signing requirements, and release destinations are decided.
